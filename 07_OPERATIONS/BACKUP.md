# BACKUP

Back up service configuration, model configuration/registry metadata, Compose definitions, Qdrant collection metadata and data according to retention policy, and generated artifacts where required.

Secrets stay outside Git.