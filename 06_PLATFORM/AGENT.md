# AGENT

Agent controller responsibilities: task understanding, planning, retrieval, tool selection, tool execution, validation, user confirmation, artifact generation, provenance and recovery/error handling.

The model must not bypass tool policy.