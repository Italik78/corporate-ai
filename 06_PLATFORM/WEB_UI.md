# WEB UI

Open WebUI remains the primary user-facing chat UI.

Previous behavior: ASK streaming worked end-to-end; COMPOSE text streaming worked; GENERAL/EDIT/FINALIZE were not fully streaming; external OpenAI connection did not render custom status events.

Open WebUI Pipe is the planned integration for richer status events. Verify against the current Open WebUI version before deployment.