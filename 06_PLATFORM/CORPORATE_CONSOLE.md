# CORPORATE AI CONSOLE

Application dashboard separate from NVIDIA DGX Dashboard.

Must show services, status, version/image, ports, network, health, CPU/RAM/GPU, dependencies, connections, logs, configuration and last health check.

Topology should update when services are added.