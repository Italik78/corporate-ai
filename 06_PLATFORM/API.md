# API

Target: OpenAI-compatible LLM API where appropriate, plus a Corporate AI API for orchestration.

The gateway owns routing, sessions, streaming, tool policy, citations, provenance and structured errors.