# VECTOR DB

Qdrant is the primary vector database.

Previous deployment had 12+ active collections. Collections must be inventoried before migration or rebuild.

Do not recreate or delete collections until schemas, counts and model compatibility are backed up.