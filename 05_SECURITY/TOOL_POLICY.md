# TOOL POLICY

Every tool declares name, version, schema, purpose, allowed inputs, permissions, side effects, resource limits, timeout, audit fields and error contract.

High-impact actions require explicit user confirmation.